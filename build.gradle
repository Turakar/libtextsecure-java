subprojects {
    ext.version_number     = "2.3.1provisioning"
    ext.group_info         = "de.thoffbauer"
    ext.signal_version     = "2.2.0"

    if (JavaVersion.current().isJava8Compatible()) {
        allprojects {
            tasks.withType(Javadoc) {
                options.addStringOption('Xdoclint:none', '-quiet')
            }
        }
    }
}
